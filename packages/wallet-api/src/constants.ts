export const DEFAULT = {
  ROLES: ['REGULAR', 'ADMIN', 'SUPER_ADMIN']
};

export enum CURRENCY {
  BTC = 'btc',
  ETH = 'eth',
  LTC = 'ltc'
}

export enum CHAIN {
  TESTNET = 'testnet',
  MAINNET = 'mainnet'
}
